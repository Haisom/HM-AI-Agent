# Domain

Core business rules, models, policies, and contracts live here.

Keep this layer pure. It should not import SDKs, MCP clients, HTTP frameworks,
databases, environment variables, or filesystem-specific code.
