# Agents

Agent roles, prompts, policies, and behavior definitions live here.

Keep role-specific logic in the closest matching folder, and move shared
coordination behavior into `orchestrator` only when it is truly cross-cutting.
